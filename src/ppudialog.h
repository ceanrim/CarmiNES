#if !defined(PPUDIALOG_H)
/* ========================================================================
    File: 
    Date: 
    Revision: 
    Creator: Carmine Foggia 
   ======================================================================== */

#define PPUDIALOG_H
#include <windows.h>

/*class PPUDialog
{
public:
        PPUDialog(unsigned short, size_t, size_t);
        bool           isOpen;
        HWND           WindowHandle;
        void           Open(HWND);
        LRESULT        WindowProc(HWND, UINT, WPARAM, LPARAM);
        unsigned      *RenderingMemory;
private:
        size_t         RenderingMemorySize;
        BITMAPINFO     RenderingMemoryInfo;
        unsigned short ResourceID;
virtual void           Refresh();
};

class CHRROMViewerClass : public PPUDialog
{
public:
    CHRROMViewerClass(unsigned short, size_t, size_t);
    void Refresh();
};

class PaletteViewerClass : public PPUDialog
{
public:
    PaletteViewerClass(unsigned short, size_t, size_t);
    void Refresh();
};

class NametableViewerClass : public PPUDialog
{
public:
    NametableViewerClass(unsigned short, size_t, size_t);
    void Refresh();
    };*/

#endif
